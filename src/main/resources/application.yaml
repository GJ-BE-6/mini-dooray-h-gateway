spring:
  mvc:
    hiddenmethod:
      filter:
        enabled: true
  application:
    name: minidorray_gateway
  messages:
    basename: message
    encoding: UTF-8
  data:
    redis:
      host: 133.186.241.167
      port: 6379
      password: "*N2vya7H@muDTwdNMR!"
      database: 30
server:
  port: 8080

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui-custom.html


